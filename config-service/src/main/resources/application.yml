server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/black-pepper/Spring-Cloud-Study.git
          branch: master
#          username: {username}
#          password: {personal access tokens}